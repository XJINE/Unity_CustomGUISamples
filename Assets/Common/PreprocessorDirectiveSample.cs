// NOTE:
// UnityEditor's code must be put in PreprocessorDirective.
// If do not, some error occur when the app building (and it fails).

#if UNITY_EDITOR
using UnityEditor;
public class PreprocessorDirectiveSample : Editor { }
#endif