using UnityEngine;

// NOTE:
// In the Scene View, when clicking to object,
// Unity will select the root of the object (means SelectionBase).

[SelectionBase]
public class SelectionBaseSample : MonoBehaviour { }